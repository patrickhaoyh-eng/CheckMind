# CheckMind 实施任务清单

## 当前范围
- 当前主线聚焦 Testlab 需要翻页的长表滑窗取证（优先 `Channel Setup` 与 `Sine Setup`）。
- 当前阶段目标已从“先跑通切页/翻页链路”切换为“人工标定驱动的长期稳定基线”。
- 本阶段验证记录见：`docs/05-Sine Setup长表滑窗验证记录.md`
- 长期基线说明见：`docs/08-人工标定统一基线.md`
- `docs/06-翻页滑窗取证范式-PgUpPgDn.md` 保留为执行层范式文档；长期主基线改由“人工标定 + 运行前核验”负责治理层。
- 长期坐标体系基线：以 `1920×1080（100% DPI）` 为统一标定与截图坐标系；正式执行前按屏规格门禁，不按“第几屏编号”。

## Task A：纵向滚动可靠性

**状态**: 已验收通过（阶段性链路闭环，保留为历史成果）

### 已完成
- [x] 固定页签点击点切到 `Sine Setup`
- [x] 固定页签验真签名命中，切页耗时保持快路径
- [x] 表格扫描前自动回顶，并在回顶不稳定时阻断正式扫描
- [x] 回顶策略：PgUp-only + 稳定性 hash 门禁（连续 2 次一致）
- [x] 翻页策略：PgDn-only + 每次翻页后立刻落一屏证据
- [x] 彻底弃用 OCR（回顶/翻页/变化检测均不依赖 OCR）
- [x] 双页滑窗测试已成立：`Channel Setup` 与 `Sine Setup` 均能产出 `coverage.json`、`table_scroll_events_*.json` 与 `stitched.png`

### 当前验收结论
- [x] 切页与翻页能力已固化，可作为后续任务基线
- [x] 该能力保留为执行层已验证动作，继续纳入长期基线
- [x] 废弃的是“运行时动态猜位置”的旧治理基线，不是 `PgUp/PgDn` 本身

## Task C：人工标定统一基线

**状态**: 进行中（Phase 1~4 主链路已跑通并固化，Phase 5 待继续清理）

### 目标
- [x] 把页签点击点、截图框、验真框统一纳入人工标定 profile
- [x] 在正式执行前对全部已标定对象做统一核验
- [x] 任一核验失败时直接阻断，不允许带病运行
- [x] 固定工位门禁按屏规格强制：Testlab 窗口所在显示器必须为 `1920×1080` 且 `100% DPI`，并启用 Per-Monitor V2 DPI 感知，避免混合 DPI 坐标虚拟化
- [ ] 在前端提供一键重新标定入口
- [x] 固化“覆盖优先、宁可重复不允许遗漏、后处理再去重”的截图采集原则

### 设计基线
- `docs/08-人工标定统一基线.md`
- `docs/06-翻页滑窗取证范式-PgUpPgDn.md`
- 开发计划：`docs/09-人工标定统一基线开发计划-V1.md`

### 推荐实施顺序
- [x] Phase 1：profile 扩展（点击点 / 截图框 / 验真框 / 弹窗点）
- [x] Phase 2：运行前统一核验与结构化核验结果
- [ ] Phase 3：前端重标入口
- [x] Phase 4：`Channel Setup` / `Sine Setup` 执行链路接入
- [ ] Phase 5：旧动态截图区域治理基线迁出

### 非目标
- [ ] 不继续扩展“运行时动态猜测截图框”这条旧路线
- [ ] 不把 OCR 重新引入长期主链路
- [ ] 不在采集阶段做激进裁汰以换取更少图片

## Task B：截屏证据质量与裁汰

**状态**: 进行中

### 目标
- [ ] 截屏证据可支撑后续核验比对（审查“截错/漏截/信息缺失”）
- [ ] 冗余截屏分类归档（核验证据 vs. 调试辅助），并形成稳定目录规范
- [ ] 截图框来源从“运行时策略推导”切换为“人工标定固定框”
- [ ] 逐步淘汰旧的动态截图区域策略

### 规范基线
- `docs/07-截屏证据质量与裁汰规范.md`

## 证据基线
- 当前最佳验收 run：`artifacts/probe-runs/581545f8274c4662b4af93c3c92bec86`
- 关键文件：
  - `coverage.json`
  - `table_scroll_events_channelparameterstable.json`
  - `screenshots/evidence/testlab_table_channelparameterstable_stitched.png`
  - `testlab_phases.log`
- 收口说明：
  - `docs/05-Sine Setup长表滑窗验证记录.md` 用于固定本阶段“已验证事实 / 当前边界 / 后续动作”。
  - `docs/08-人工标定统一基线.md` 用于固定当前长期方向与旧基线废弃口径。
  - 本轮已收口：`Sine Setup` 页签验真签名一次命中、`top_serial` 两张表均达到 `configured=1; matched=1`。
