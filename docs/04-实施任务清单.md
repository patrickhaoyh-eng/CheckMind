# CheckMind 实施任务清单

## 当前范围
- 当前主线聚焦 Testlab 需要翻页的长表滑窗取证（优先 `Channel Setup` 与 `Sine Setup`）。
- 当前阶段目标已从“先跑通切页/翻页链路”切换为“人工标定驱动的长期稳定基线”。
- 当前对齐原则：允许实现路径因实机收敛而调整，但顶层质量约束、证据要求、fail-fast、固定工位边界与非目标不变。
- 本阶段验证记录见：`docs/05-Sine Setup长表滑窗验证记录.md`
- 长期基线说明见：`docs/08-人工标定统一基线.md`
- `docs/06-翻页滑窗取证范式-PgUpPgDn.md` 保留为执行层范式文档；长期主基线改由“人工标定 + 运行前核验”负责治理层。
- 长期坐标体系基线：以 `1920×1080（100% DPI）` 为统一标定与截图坐标系；正式执行前按屏规格门禁，不按“第几屏编号”。

## Task A：纵向滚动可靠性

**状态**: 已验收通过（阶段性链路闭环，保留为历史成果）

### 已完成
- [x] 固定页签点击点切到 `Sine Setup`
- [x] 固定页签验真签名命中，切页耗时保持快路径
- [x] 表格扫描前自动回顶，并在回顶不稳定时阻断正式扫描
- [x] 回顶策略：PgUp-only + 稳定性 hash 门禁（连续 2 次一致）
- [x] 翻页策略：PgDn-only + 每次翻页后立刻落一屏证据
- [x] 彻底弃用 OCR（回顶/翻页/变化检测均不依赖 OCR）
- [x] 双页滑窗测试已成立：`Channel Setup` 与 `Sine Setup` 均能产出 `coverage.json`、`table_scroll_events_*.json` 与 `stitched.png`

### 当前验收结论
- [x] 切页与翻页能力已固化，可作为后续任务基线
- [x] 该能力保留为执行层已验证动作，继续纳入长期基线
- [x] 废弃的是“运行时动态猜位置”的旧治理基线，不是 `PgUp/PgDn` 本身

## Task C：人工标定统一基线

**状态**: 进行中（Phase 1~4 主链路已跑通并固化；“统一主链函数收口 + 旧页面回归”已完成，`Notch Profile` 已接回正式业务链路并完成正式回归；页签验真底层漂移已补齐正式留档证据，当前主线已切回业务扩展与对象补齐）

### 目标
- [x] 把页签点击点、截图框、验真框统一纳入人工标定 profile
- [x] 在正式执行前对全部已标定对象做统一核验
- [x] 任一核验失败时直接阻断，不允许带病运行
- [x] 固定工位门禁按屏规格强制：Testlab 窗口所在显示器必须为 `1920×1080` 且 `100% DPI`，并启用 Per-Monitor V2 DPI 感知，避免混合 DPI 坐标虚拟化
- [ ] 在前端提供一键重新标定入口
- [x] 固化“覆盖优先、宁可重复不允许遗漏、后处理再去重”的截图采集原则

### 设计基线
- `docs/08-人工标定统一基线.md`
- `docs/06-翻页滑窗取证范式-PgUpPgDn.md`
- 开发计划：`docs/09-人工标定统一基线开发计划-V1.md`

### 推荐实施顺序
- [x] Phase 1：profile 扩展（点击点 / 截图框 / 验真框 / 弹窗点）
- [x] Phase 2：运行前统一核验与结构化核验结果
- [x] Phase 4：`Channel Setup` / `Sine Setup` 执行链路接入
- [x] 统一主链函数收口：把 `Sine Setup` / `Channel Setup` 共用的翻页抓取流程拆成标准函数，并为差异页面预留前置激活接口
- [x] 旧页面回归：`Sine Setup` / `Channel Setup` 在函数收口后重新跑通，作为继续开发 `Notch Profile` 的前置门禁
- [x] `Notch Profile` 差异前置层回归：正式 table-scan probe 连续两轮通过（run `6ff5dfa9169f436087e7963dca8d2a37`、`d97816fd3fb64d2fb271f1e4ef523b5d`）
- [x] `Notch Profile` 正式业务链路接回：已完成 runner 接线、双点恢复标定与正式链路首轮验证通过（run `8c03cedac5dd438183534a109647606d` 达到 `ChunkCount=3 / UniqueChunkCount=3 / ChangedEventCount=2 / IsConsistent=true`）
- [x] 页签验真底层漂移已做最小修复：运行时验真改为稳定采样，`Sine Setup` 验真改为“先切回 `Channel safety parameters` 基面再验真”，标定器与运行器口径已对齐
- [ ] 正弦专项：待抓取素材与人工标定对象补齐（在统一主链回归通过后继续）
- [ ] Task B：证据裁汰、拼接优化与 profile 主口径整理（作为正弦专项后的 cleanup 收口）
- [ ] Phase 5：旧动态截图区域治理基线迁出
- [ ] Phase 3：前端重标入口（在 cleanup 收口后补齐）

### 正弦专项当前收口口径
- [ ] 把已经完成的 `Channel Setup` / `Sine Setup` 与新增对象统一纳入一张“抓取通用范式与差异化配置项表”
- [ ] 先把 `Sine Setup` / `Channel Setup` 的共用步骤收口为标准函数主链：切页验真、入口定位、回顶、翻页、变化检测、去重拼接、证据落盘
- [x] `Notch Profile` 的差异先只拆“进入可翻页状态 / 获取可发键焦点”这一层，不提前改写后半段统一主链
- [x] `Notch Profiles` 的 `1..N` 列表不做“每行单独人工标定”，改为“固定列表布局 + 行定位规则 + `Edit` 入口”
- [x] `Notch Profile` 相关抓取按 `Notch Profiles` 序号驱动循环执行：逐个序号进入、执行、返回，直到当前任务要求的全部序号完成
- [x] 后端任务入参口径已固定：正弦任务模板使用内部字段 `notchProfileCount`，运行时映射到 `CHECKMIND_NOTCH_PROFILE_COUNT` 并展开为 `1..N`；原有 `INDEX / INDEXES` 仅保留为调试兼容入口
- [x] `notchProfileCount` 数量不符时已形成正式运行口径：若目标序号连续两次点击后仍停留在上一条有效列表项，则不再重复打开旧条目，按实际列表数量正常结束，并向用户弹出“`Notch Profile Count` 与实际列表数量不符”的确认提示；正式验证 run `2523cbe57bb44a3188258345fe02e987` 已完成该场景留档
- [x] `Channel safety parameters -> Define notch profiles` 两步入口与 `Notch Profiles` 列表入口标定对象已存在于 `workstation_profile.json`，当前下一步不是重新标定，而是基于现有对象做正式留档/验证
- [x] `Channel safety parameters -> Define notch profiles -> Notch Profiles(row=1)` 已完成正式 probe 留档验证：run `7a7e95bccade48c0a1331e77e3e4a53d` 中已成功进入 `Notch Profile : 30g`、完成表格扫描（`chunks=3 / unique=3`）、关闭子窗口并返回列表
- [x] `Define notch profiles` 的布局签名标定入口已补齐并完成实机留档：标定 run `a53d4754b9d541ddb710f72ec0d4a910` 已确认 `define_notch_profiles.VerifyTargets.notch_profiles_layout` 可成功写回 `workstation_profile.json`
- [x] `Notch Profiles` 第二刀 preflight 门禁已完成正式口径调整：初版内容级硬签名在真实扩展到第 3 条内容后暴露出“动态列表内容不适合作为硬签名”的设计缺陷；当前已改为“几何/流程门禁放行 + 哈希漂移留证不阻断”，正式 run `a0f15376086c46fda4d7de43943d6b0e` 已确认 `row=1/2/3` 三个子窗口都可完成扫描闭环
- [x] `Sine Setup -> Channel safety parameters` 恢复链已从可疑单点修正为两步序列，并补齐负向校验；正式 run `f7e209f91e1845f78fed03d45b64ed3a` 中两次均记录 `runner.sine_setup_channel_safety_restored | sequence=(1086,147)->(1077,174)`
- [x] `Notch Profiles` 当前正式业务链路已完成单序号闭环：正式 run `f7e209f91e1845f78fed03d45b64ed3a` 的 `results.json` 为 `status=completed`、`completedNotchProfileCount=1`，说明第二刀门禁放行后业务主链已恢复
- [x] `Notch Profiles` 模板口径 `notchProfileCount=2` 已完成正式验证：在门禁前新增“列表选中态归一化到首行”后，正式 run `e2aab77a4ffe4b3ab5c9ca7eab273f2c` 已确认 `runner.notch_profiles_layout_selection_normalized` 与 `runner.notch_profiles_layout_signature_matched` 同时命中，`results.json` 写出 `completedNotchProfileCount=2 / requestedNotchProfileCount=2`
- [x] `Profile Editor` 与 `Advanced Control Setup` 的子窗口标定入口已补齐，相关对象已完成实机标定并写回 `workstation_profile.json`；其中包含 `Profile Editor` 滑窗 ROI、`Advanced Control Setup` 三个子页签 ROI，以及 `Sine Setup` 右侧 `Control` 栏 ROI
- [x] `Sine Setup` 已接入固定区域留档，且正式验证 run `217414b316674f4f8f4a5cc054d24cd1` 已完整输出 `control_panel` 主页面固定截图，以及 `Profile Editor`、`Advanced Control Setup` 子窗口固定截图结果
- [ ] 对可调整子窗口统一执行“先最大化，再抓取 / 翻页 / 关闭”的通用范式
- [x] `Profile Editor` 已改为在子窗口内复用统一的 `PgUp/PgDn + 拼接 + 去重` 表格扫描能力；当前实现会把 `profile_editor.table_scan` 作为真正的翻页表处理，不再只截第一页固定 ROI
- [x] `Profile Editor` 翻页稳定性已补齐正式实机验证：前置重标 run `5a823e1a18c54854ad772ccc1b258b93` 后，正式业务 run `8f91b14758d34f42ae6ddb8564d26cfd` 已确认 `Profile Editor` 可稳定形成 `TableScans.Chunks=3 / UniqueChunkCount=3`、`stitched.png` 正常落盘，且不再触发 `Testlab EXCEPTION_ACCESS_VIOLATION`
- [x] `Profile Editor` 当前稳定口径已更新并完成独立复跑：使用人工标定的空白分页点（`PagingFocusPointWindow` / `PagingActivationPointWindow`）后，需先执行一次 bootstrap `PgDn` 激活翻页语义，再 `PgUp` 回顶，最后进入正式两次 `PgDn`；正式验证 run `718dc8781c074e79ae8aed839ef19f7d` 已确认该链路生效
- [ ] 若列表行高、列宽或布局漂移导致变量行定位规则失效，preflight 直接阻断
- [ ] 已稳定的 `Channel Setup` / `Sine Setup` 主链路只做兼容式扩展，不得改失效
- [x] 本轮不以 `Notch Profile` 闭环跑通为验收目标；先以旧页面回归通过作为继续扩面的门禁
- [x] `Notch Profile` 正式业务链路连续两轮稳定通过且不破坏现有稳定性
- [x] 补一份不受沙箱限制影响的正式留档 run，作为“页签验真底层漂移已修复”的最终证据包（前置重标 run `34624ca3b7904e219a8bf8edefe311a2` 后，正式业务 run `8de6db648e2a44e4b64f9b196299efaa` 已完整落盘，`NotchProfileIndexes='1,2'` 均通过）

### 非目标
- [ ] 不继续扩展“运行时动态猜测截图框”这条旧路线
- [ ] 不把 OCR 重新引入长期主链路
- [ ] 不在采集阶段做激进裁汰以换取更少图片

## Task B：截屏证据质量与裁汰

**状态**: 次优先级（待统一主链函数收口与旧页面回归通过后继续）

### 目标
- [ ] 截屏证据可支撑后续核验比对（审查“截错/漏截/信息缺失”）
- [ ] 冗余截屏分类归档（核验证据 vs. 调试辅助），并形成稳定目录规范
- [ ] 截图框来源从“运行时策略推导”切换为“人工标定固定框”
- [ ] 逐步淘汰旧的动态截图区域策略
- [ ] 拼接与裁汰逻辑与 `coverage.json`、`table_scroll_events_*.json` 保持一致，避免“看起来有图但不利于人工复核”
- [ ] `workstation_profile.json` 继续向结构化主口径收敛，旧字段只保留兼容兜底

### 规范基线
- `docs/07-截屏证据质量与裁汰规范.md`

### 当前收口重点
- `evidence/` 与 `debug/` 的用途边界清晰，不混用
- 拼接结果可真实反映逐屏顺序与覆盖推进
- profile 主写入口优先使用结构化字段（尤其 `VerifyTargets`）
- 已新增 `table_evidence_*.json`，用于固化“去重键 / unique chunk 数 / scroll events 一致性”报告
- 为后续 `Phase 5` 清理旧动态截图区域治理基线提供文档与实现基础

## 证据基线
- 当前最佳验收 run：`artifacts/probe-runs/8de6db648e2a44e4b64f9b196299efaa`
- 关键文件：
  - `coverage.json`
  - `table_scroll_events_channelparameterstable.json`
  - `table_scroll_events_notchprofiletable#1.json`
  - `table_scroll_events_notchprofiletable#2.json`
  - `table_evidence_notchprofiletable#1.json`
  - `table_evidence_notchprofiletable#2.json`
  - `screenshots/evidence/testlab_table_channelparameterstable_stitched.png`
  - `testlab_phases.log`
- 收口说明：
  - `docs/05-Sine Setup长表滑窗验证记录.md` 用于固定本阶段“已验证事实 / 当前边界 / 后续动作”。
  - `docs/08-人工标定统一基线.md` 用于固定当前长期方向与旧基线废弃口径。
  - 本轮已收口：`Sine Setup` 页签验真签名一次命中、`top_serial` 两张表均达到 `configured=1; matched=1`。
  - `top_serial` 已固化结论：样本生成时机必须与正式运行一致；该结论已进入长期基线文档。
- 页签验真专项补充结论：
  - 运行时改为稳定采样，不再用单帧直接判定 `profile_signature_mismatch`
  - `Sine Setup` 的验真对象已固定为 `Channel safety parameters` 基面，避免把中间态误写入 profile
  - `34624ca3b7904e219a8bf8edefe311a2` 已确认为“前置重标 run”，不作为正式业务证据；其后续正式业务 run `8de6db648e2a44e4b64f9b196299efaa` 已完整进入 `runner` 并正常落盘
  - 本轮本机正式留档已确认“不受沙箱阻断”：不再弹验真签名不中对话框，`Sine Setup` 可正常切页进入正式链路，`Notch Profile #1/#2` 均完成进入、扫描、关闭与回到列表的完整闭环
  - 当前固定状态新增验证基线：`24095a0b2a6f409a928bd3cf08216b56` 为前置重标 run，不作为正式业务证据；其后正式验证 run `217414b316674f4f8f4a5cc054d24cd1` 已完整进入 `runner` 并正常落盘，`Channel Setup` / `Sine Setup` 主表、`control_panel`、`Profile Editor`、`Advanced Control Setup` 三个子页签均留档成功
  - `Notch Profiles` 入口新增验证基线：run `7a7e95bccade48c0a1331e77e3e4a53d` 已确认 `Channel safety parameters -> Define notch profiles -> Notch Profiles(row=1)` 可稳定进入 `Notch Profile` 编辑窗口，完成 `chunks=3 / unique=3` 的表格扫描、关闭子窗口并回到列表
  - `Define notch profiles` 第二刀门禁新增验证基线：标定 run `a53d4754b9d541ddb710f72ec0d4a910` 已将 `notch_profiles_layout` 写回 profile；当前正式口径已改为“几何/流程门禁 + 哈希漂移留证”，run `a0f15376086c46fda4d7de43943d6b0e` 已确认 `runner.notch_profiles_layout_hash_mismatch_ignored` 与 `runner.notch_profiles_layout_geometry_verified` 后，`row=1/2/3` 三个子窗口均可正常扫描
  - 显式索引成功口径新增验证基线：run `756a6913f4794edcb4eceda885762a75` 已确认 `NotchProfileIndexes='1,2,3'` 在当前新门禁下可完整完成 `row=1/2/3` 三个子窗口扫描，`results.json` 写出 `status=completed / completedNotchProfileCount=3 / alerts=[]`
  - `Notch Profiles` 第二刀门禁的修正结论已固定：初版签名 ROI 含首行选中态，导致运行时因选中样式差异触发假阳性 `mismatch`；现已改为基于 `FirstRowAnchor + RowHeight` 从第二行稳定区域起签名，避免把首行蓝底/焦点框渲染差异误判为布局漂移
  - `Notch Profiles` 模板口径新增验证基线：run `e2aab77a4ffe4b3ab5c9ca7eab273f2c` 已确认当任务通过 `notchProfileCount=2` 驱动而非显式 `Indexes='1,2'` 时，门禁前的首行归一化可消除列表选中态差异，正式结果写出 `completedNotchProfileCount=2 / requestedNotchProfileCount=2`
  - `Sine Setup -> Channel safety parameters` 恢复链新增验证基线：单点恢复序列已废弃，当前正式口径为两步序列 `(1086,147)->(1077,174)`；运行器同时补充“仍停留在 `Define notch profiles` 布局则判恢复失败”的负向校验，避免把点击过了误写成已恢复
  - `notchProfileCount` 数量不符新增验证基线：run `860d5a4d37bc47378064fd8f07870528` 已确认当用户输入 `NotchProfileCount=4`、实际仅有 3 条列表项时，第二刀门禁不会提前误拦；正式链路会先完成 `row=1/2/3` 的扫描与关闭返回，再在 `row=4` 写出 `NotchProfileCountMismatch`、终端输出 `notch_profile_count_mismatch=requested:4;completed:3;failedRow:4`，并弹出中文确认提示框
  - `Profile Editor` 翻页补齐已形成新的正式实机验证基线：前置重标 run `5a823e1a18c54854ad772ccc1b258b93` 后，正式业务 run `8f91b14758d34f42ae6ddb8564d26cfd` 已确认 `Profile Editor` 肉眼翻页正常、截图完整落盘，`testlab_run.json` 中 `ChildWindowCaptures.TableScans` 写出 `Chunks=3 / UniqueChunkCount=3`，`screenshots/evidence/testlab_table_profileeditortablescan_stitched.png` 已生成，现场未再出现 `Testlab EXCEPTION_ACCESS_VIOLATION`
